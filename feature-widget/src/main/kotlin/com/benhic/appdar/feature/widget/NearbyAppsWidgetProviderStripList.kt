package com.benhic.appdar.feature.widget

/** Strip List variant — vertical list of icon + name rows, pre-sized for ~3×5 cells. Registered separately for the widget picker. */
class NearbyAppsWidgetProviderStripList : NearbyAppsWidgetProvider()
