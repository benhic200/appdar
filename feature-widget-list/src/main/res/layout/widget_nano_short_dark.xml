<?xml version="1.0" encoding="utf-8"?>
<!--
    Short nano widget (dark): horizontal layout for widgets ≤ 90dp tall (e.g. 128×72dp / 2×1).
    Icon on left, name + distance on right. No chip background — space is too tight.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/nano_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_ripple_row_dark"
    android:clickable="true"
    android:focusable="true"
    android:padding="6dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/nano_app_icon"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:contentDescription="@string/app_icon_description"
            android:importantForAccessibility="no" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:paddingStart="8dp">

            <TextView
                android:id="@+id/nano_business_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:maxLines="1"
                android:ellipsize="end"
                android:textColor="#FFFFFFFF"
                android:textSize="11sp"
                android:textStyle="bold" />

            <LinearLayout
                android:id="@+id/nano_chip_row"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:layout_marginTop="2dp">

                <TextView
                    android:id="@+id/nano_distance"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="#FFCE93D8"
                    android:textSize="9sp"
                    android:maxLines="1" />

                <ImageView
                    android:id="@+id/nano_status_dot"
                    android:layout_width="6dp"
                    android:layout_height="6dp"
                    android:layout_marginStart="4dp"
                    android:importantForAccessibility="no" />
            </LinearLayout>
        </LinearLayout>
    </LinearLayout>

    <!-- Settings button pinned to top-left corner -->
    <ImageView
        android:id="@+id/nano_settings"
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:layout_gravity="top|start"
        android:src="@drawable/ic_settings_gear"
        android:contentDescription="Open Appdar"
        android:padding="2dp"
        android:tint="#FFCE93D8"
        android:clickable="true"
        android:focusable="true"
        android:background="@drawable/widget_ripple_icon" />

    <!-- Tiny refresh button pinned to top-right corner -->
    <ImageView
        android:id="@+id/nano_refresh"
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:layout_gravity="top|end"
        android:src="@android:drawable/ic_menu_rotate"
        android:contentDescription="@string/refresh"
        android:padding="2dp"
        android:tint="#FFCE93D8"
        android:clickable="true"
        android:focusable="true"
        android:background="@drawable/widget_ripple_icon" />

</FrameLayout>
